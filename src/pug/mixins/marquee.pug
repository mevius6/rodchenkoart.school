mixin marquee(props)
  .marquee
    .marquee__inner(aria-hidden="true" style={
      '--anim-direction': props.animDir || 'normal'
    })
      each val of Array(props.iter || 4)
        span= props.text
